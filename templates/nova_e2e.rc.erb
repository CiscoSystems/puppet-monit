check program nova_e2e with path "/opt/monit/scripts/nova_e2e.bash"
    every 30 cycles 
    if status != 0 then exec "/opt/monitplugins/sys/processdown nova_e2e CRITICAL <%= bmc_server %> <%= event_class %>"
