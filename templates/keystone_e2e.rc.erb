check program keystone_e2e with path "/opt/monit/scripts/keystone_e2e.bash"
        if status != 0 then exec "/opt/monitplugins/sys/processdown keystone_e2e CRITICAL <%= bmc_server %> <%= event_class %>"

